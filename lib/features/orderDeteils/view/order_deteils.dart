import 'package:flutter/material.dart';

class OrderDeteils extends StatelessWidget {
  const OrderDeteils({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}