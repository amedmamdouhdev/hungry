import 'package:flutter/material.dart';

class CheachoutView extends StatelessWidget {
  const CheachoutView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}